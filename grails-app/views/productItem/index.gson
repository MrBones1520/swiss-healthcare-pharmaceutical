model{
    List<ProductItem> products = []
    int stockInCount = 0
    int stockOutCount = 0
    int saleOutCount = 0
}

json {
    total g.render([stockInCount, stockOutCount, saleOutCount])
    items g.render(products)
}