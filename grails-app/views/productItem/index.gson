model{
    List<ProductItem> products = []
    int stockIn = 0
    int stockOut = 0
    int saleOut = 0
}

json {
    total g.render([products.size(), stockIn, stockOut, saleOut]){ value it }
    items g.render(products)
}