model{
    List<ProductItem> products = []
    int stockIn = 0
    int stockOut = 0
    int saleOut = 0
}

json {
    total g.render([stockIn, stockOut, saleOut]){ value it }
    items g.render(products)
}