model {
    ProductItem productItem
}

json {
    barcode productItem.barcode
    base g.render(productItem.base)
    status g.render(productItem.status, [excludes: ['description']])
    assigned productItem.assigned
    dateCreated productItem.dateCreated
    lastUpdated productItem.lastUpdated
}