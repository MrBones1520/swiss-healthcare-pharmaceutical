model {
    ProductItem productItem
}

json {
    barcode productItem.ident()
    dateCreated productItem.dateCreated
    base g.render(productItem.descriptor)
    status g.render(productItem.status)
}